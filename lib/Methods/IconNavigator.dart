import 'package:flutter/material.dart';

class Iconnavigator extends StatelessWidget {
  const Iconnavigator({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}